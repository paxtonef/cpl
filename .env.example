# Application
APP_ENV=development
LOG_LEVEL=INFO

# PostgreSQL
DATABASE_URL=postgresql://cpl_user:cpl_pass@localhost:5432/cpl_db

# Connection pool
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_ECHO=false

# Runner placeholders — not used in B1
VIR_ENDPOINT=
PGDR_ENDPOINT=
VIR_TIMEOUT=30
PGDR_TIMEOUT=60
